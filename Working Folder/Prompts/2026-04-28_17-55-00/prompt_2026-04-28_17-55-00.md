# Prompt — 2026-04-28 17:55:00 (synthetic timestamp)

> Sorry I meant Readme.md
>
> [referring to the previous request:]
> > [Changelog.md](http://Changelog.md) in the root (/Users/Alex/Dev/Github/Achievement) can be deleted, this doesn't need to be part of the build process output. It can still be output in the Cache and Tools folders with each query.

## What was actually being asked

Correction to the previous prompt — the file at the repo root the user wants deleted is `README.md`, not `CHANGELOG.md`. There IS a `README.md` at the repo root (31 bytes, just `# Achievement\nAchievement Data\n` — placeholder from the initial commit, tracked in git). The user wants this gone, and wants README.md to continue appearing inside the per-prompt `Cache and Tools_{timestamp}/` snapshots.

## Action

1. ✅ `README.md` at the repo root — removed from git's perspective (`git status` shows `D README.md`). Sandbox couldn't `rm` due to the same macOS-lock issue that's blocked `.DS_Store` and `.pyc` deletions all session, so renamed to `_README_DELETE_ME` for the user to drag to Trash via Finder. The git-tracked deletion is what matters; the rename is just bookkeeping.
2. ✅ Per-prompt snapshot inclusion — already in place. Every existing session-2 capsule (`17-00-01`, `17-05-00`, `17-10-00`, `17-15-00`, `17-20-00`, `17-30-00`, `17-45-00`) already has a `Cache and Tools_{ts}/README.md` (the tooling README from `Working Folder/Cache and Tools/`). The snapshot-build pattern in `rebuild.command`-adjacent operations continues copying it on each new capsule.

## What was NOT touched (left alone unless you say otherwise)

- `Working Folder/README.md` (3,920 bytes — the Working Folder readme, separate from the repo-root one)
- `Working Folder/Cache and Tools/README.md` (the master tooling README, currently 4,884 bytes — what gets snapshotted)
- `Working Folder/Cache and Tools/project_docs/README.md` (the original project overview from `achievement_project/README.md`, archival)

If you want any of those gone too, let me know which.

## Cleanup queue (Finder)

Add to the existing list:
- `_README_DELETE_ME` at repo root

Plus the prior session's leftovers (`_ACHIEVEMENT_PROJECT_DELETE_ME`, `_OLD_JUNK_DELETE_ME`, `_PYCACHE_*_DELETE_ME`, `_DASHBOARD_DELETE_ME`, `_index_html_DELETE_ME`, `_xlsx_DELETE_ME`, `_BEFORE_REBUILD_xlsx_DELETE_ME`).

## Cross-references

- Audit trail: `Working Folder/CHANGELOG.md`
- Tooling inventory: `Working Folder/Cache and Tools/README.md`

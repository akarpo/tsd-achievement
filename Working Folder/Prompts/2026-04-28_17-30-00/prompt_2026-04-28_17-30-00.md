# Prompt — 2026-04-28 17:30:00 (synthetic timestamp)

> I want you validate that all Cache and Tools in /Dev/Github/Achievement have no dependency on data stored in OneDrive. All the data is local, and the file generation is within that folder. Can achievement_project be deleted? I want all work to be taking place in Cache and Tools

## Two asks in one

1. **Validate that `Cache and Tools/` has no OneDrive dependencies** — local-only, self-contained.
2. **Can `achievement_project/` be deleted?** Move toward a layout where everything happens inside `Cache and Tools/`.

## Validation findings

**OneDrive: clean.** Source-code grep across `Cache and Tools/` returned zero hits for `OneDrive`, `CloudStorage`, `/Users/Alex/Library`, or any hardcoded `/Users/...` path. No script does file I/O against an absolute host path — everything goes through `_paths.py` constants resolved off `__file__`. The only `/sessions/...` references were in `_PYCACHE_DELETE_ME/*.pyc` — Python's normal bytecode cache from a prior run; gets regenerated fresh each time, not a runtime dependency.

**achievement_project: cannot be deleted as-is.** While the OneDrive question came up clean, there was a separate runtime dependency on `Working Folder/achievement_project/` — every `_paths.py` constant pointed at sibling-of-Working-Folder/achievement_project/. Specifically:
- `master_demographics.csv` (5,917-row canonical input) lived in `achievement_project/extracted_data/`
- All per-state extracted CSVs lived in `achievement_project/extracted_data/{state}/`
- MI raw source data lived in `achievement_project/source_data/`
- Build outputs were written to `achievement_project/dashboard/`

Delete `achievement_project/` without moving the data, and `rebuild.command` fails on first read.

## Action taken

To enable deletion + consolidate everything in `Cache and Tools/`:

1. ✅ Moved `achievement_project/{extracted_data, source_data, dashboard, spreadsheets}/` → `Cache and Tools/{extracted_data, source_data, dashboard, spreadsheets}/`
2. ✅ Moved `achievement_project/{README, CONVERSATION_SUMMARY, HISTORY_INDEX}.md` → `Cache and Tools/project_docs/`
3. ✅ Renamed `achievement_project/scripts/` → `_PRE_PORT_SCRIPTS_DELETE_ME/` (pre-port copies, redundant with canonical `Cache and Tools/*.py`)
4. ✅ Rewrote `_paths.py`: `PROJECT = HERE` (Cache and Tools/ itself), all subfolders inside HERE. Added new `HANDOFF_*` and `DEPLOY_*` constants for the deploy flow.
5. ✅ Rewrote `rebuild.command` for the new layout — finds canonical dashboard at `HERE/dashboard/` instead of the old sibling-and-down hop.
6. ✅ Rewrote `Cache and Tools/README.md` to document the consolidated structure.
7. ✅ End-to-end validation — ran `rebuild.command`, hash equality (`a77d4426...`) across all 3 deploy locations, 5,497 All Demographics rows, Bellevue 2025 G3 Math All Students = N=1,345, 69.4%.
8. ⚠ → ✅ `achievement_project/` could not be `rm -rf`'d (locked `.DS_Store` + `.pyc` files, same macOS issue as before). Renamed to `Working Folder/_ACHIEVEMENT_PROJECT_DELETE_ME` for user to delete via Finder.

## Final layout

```
Achievement/                                    ← git repo root
├── index.html                                  ← GH Pages deploy
├── G3-G7_Achievement_Data.xlsx                 ← GH Pages deploy
└── Working Folder/
    ├── G3-G7_Achievement_Data.xlsx             (handoff copy)
    ├── index.html                              (handoff copy)
    ├── README.md, CHANGELOG.md
    ├── Cache and Tools/                        ← EVERYTHING IS HERE
    │   ├── _paths.py, rebuild.command, README.md
    │   ├── *.py (10 scripts)
    │   ├── extracted_data/                     ← input + master CSVs
    │   ├── source_data/                        ← raw downloads (MI only in repo)
    │   ├── dashboard/                          ← canonical xlsx + html outputs
    │   ├── spreadsheets/                       ← intermediate analytical files
    │   └── project_docs/                       ← README, CONVERSATION_SUMMARY, HISTORY_INDEX
    ├── Prompts/                                ← per-prompt capsules (19 total)
    └── _ACHIEVEMENT_PROJECT_DELETE_ME/         ← user deletes via Finder
```

## What's left for the user (Finder cleanup)

- `Working Folder/_ACHIEVEMENT_PROJECT_DELETE_ME/`
- `Working Folder/Cache and Tools/_PYCACHE_DELETE_ME/`
- `Working Folder/Cache and Tools/_PYCACHE_NEW_DELETE_ME/` (regenerated by today's rebuild)
- `Working Folder/_OLD_JUNK_DELETE_ME/` (left over from session 2)

Any `__pycache__` Python regenerates can be ignored — recommend adding `__pycache__/` to `.gitignore` once you decide to commit.

## Cross-references

- Tooling inventory: `Working Folder/Cache and Tools/README.md`
- Prior session capsules: `Prompts/2026-04-28_14-* … 17-20-00`
- Audit trail: `Working Folder/CHANGELOG.md`

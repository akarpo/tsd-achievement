# Prompt — 2026-04-28 17:05:00 (synthetic timestamp)

> Yes, rebuild the .xslx. Then I want you to move the 'achievement_project' folder into the 'Working Files' folder, move the .md files into the working files folder. Then I want you to validate you have all the build capability for maintaining and updating this project that are not dependent on the 'achievement_project' folder, and that all your tools you're using are captured in a newly created 'Cache and Tools' folder, and that each prompt you're creating a new .md file with date/hour/minute/second into a prompt file folder, with a new folder for each prompt, and a timestamped folder 'Cache and Tools' folder that contains all the build tools. Before doing this I want you to explain that you understand the logic of this hierarchy and structure.

## The substantive instruction this whole capsule is built around

This is the prompt that defines the project's working hierarchy going forward. Five intertwined asks:

1. **Rebuild the xlsx** — produce a fresh `G3-G7_Achievement_Data.xlsx` with the corrected Bellevue 2022/2024/2025 SBAC values. (Prior session left this incomplete due to OneDrive locks.)

2. **Move `achievement_project/` into `Working Folder/`** — folding the data payload inside the working envelope. ("Working Files" was a typo for "Working Folder", confirmed in the next prompt.)

3. **Move the `.md` prompt files into Working Folder** — clean up the project root.

4. **Validate build capability is independent of `achievement_project/`** — the build tooling should not be buried inside the data payload; it should live in a self-contained `Cache and Tools/` folder that resolves the project payload via relative paths only.

5. **Per-prompt traceability** — every prompt going forward gets a timestamped folder under `Working Folder/Prompts/{YYYY-MM-DD_HH-MM-SS}/` containing:
   - The prompt's `.md` file
   - A timestamped `Cache and Tools_{YYYY-MM-DD_HH-MM-SS}/` snapshot of all build tools as they existed at that moment

## Pre-action requirement

User asked me to first explain my understanding of the hierarchy before executing. I responded with a tree diagram and reasoning (one master `Cache and Tools/` at Working Folder root + per-prompt snapshots inside each prompt folder), then listed three open ambiguities before proceeding.

## Action sequence (eventually executed across this prompt + the next two clarifications)

1. ✅ Rebuilt `G3-G7_Achievement_Data.xlsx` using `build_master_xlsx.py` against the Bellevue-corrected `master_demographics.csv`. Verified Bellevue 2025 G3 Math All Students = N=1,345, 69.4% (the corrected SBAC value).
2. ✅ Created `Working Folder/Cache and Tools/` and `Working Folder/Prompts/`.
3. ✅ Moved `achievement_project/` from repo root into `Working Folder/achievement_project/`.
4. ✅ Retro-folded all 13 prior-session `.md` prompt files into `Working Folder/Prompts/{timestamp}/` capsules.
5. ✅ Moved `running.md` into `Working Folder/Prompts/running.md` as the chronological session log.
6. ✅ Copied all build scripts from `achievement_project/scripts/` into `Working Folder/Cache and Tools/`, patched each to import a shared `_paths.py` resolver, replaced hardcoded `/sessions/wizardly-dazzling-tesla/mnt/outputs/...` paths with portable constants.
7. ✅ Rewrote `rebuild.command` to live in Cache and Tools/ and update three deploy locations (canonical dashboard/, Working Folder/ handoff copy, repo root for GH Pages).
8. ✅ End-to-end validation by running the new rebuild.command — confirmed identical xlsx hashes across all three deploy locations, with 5,497 All Demographics rows and the corrected Bellevue value.
9. ⚠ Recovered from a self-inflicted regression — my "import check" exec'd `build_master_demographics.py`, which silently overwrote the canonical master CSV with a 5,232-row version (missing MI Troy detail). Restored from the OneDrive copy and added an `⚠ AUDIT WARNING` header to the script.

## Cross-references

- Diagnosis & summary of session 1 (Bellevue fix): `achievement_project/CONVERSATION_SUMMARY.md`, `Working Folder/CHANGELOG.md`
- Prior session prompt history: `Prompts/2026-04-28_14-*` through `2026-04-28_16-*`
- Clarifications/confirmations on this instruction: next two capsules (`17-10-00`, `17-15-00`)
- Continuation prompt that finished the work: `17-20-00`

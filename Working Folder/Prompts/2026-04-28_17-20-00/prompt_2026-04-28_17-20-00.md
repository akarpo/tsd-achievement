# Prompt — 2026-04-28 17:20:00 (synthetic timestamp)

> Continue from where you left off.

## Context

User issued this after I had:

- Rebuilt the xlsx with corrected Bellevue values (verified).
- Moved `achievement_project/` into `Working Folder/`.
- Retro-folded 13 historical prompt `.md` files into `Working Folder/Prompts/{timestamp}/` capsules.
- Created the `Cache and Tools/` skeleton with `rebuild.command` + `patch_xlsx_in_place.py`.
- Inspected each script's path-resolution pattern in preparation for migration.

The pause came mid-script-audit, so this prompt was the cue to push through the remaining work.

## Action taken (the rest of the restructure)

1. ✅ Created `Working Folder/Cache and Tools/_paths.py` — shared portable path resolver. Defines `PROJECT`, `MASTER_CSV`, `DASHBOARD_XLSX`, per-state CSV constants, and source-data folder constants.
2. ✅ Copied all .py scripts from `achievement_project/scripts/` into `Working Folder/Cache and Tools/`.
3. ✅ Patched each script to `from _paths import ...` and replaced hardcoded `/sessions/wizardly-dazzling-tesla/mnt/outputs/...` paths with the portable constants.
4. ✅ Rewrote `rebuild.command` for the new layout — lives in Cache and Tools/, updates three deploy locations (canonical dashboard/, Working Folder/ handoff, repo root for GH Pages).
5. ✅ Wrote `Cache and Tools/README.md` — full inventory + quick start + path-indirection rationale.
6. ✅ End-to-end validation — ran the new `rebuild.command`, confirmed identical xlsx hashes across all three deploy locations, 5,497 All Demographics rows, Bellevue 2025 G3 Math All Students = N=1,345, 69.4% (correct).
7. ⚠ → ✅ **Self-inflicted regression and recovery.** A `for f in ... ; python3 -c "...exec_module..."` step I ran as a "syntax/import sanity check" actually executed each script's top-level code — including `build_master_demographics.py`, which overwrote the canonical `master_demographics.csv` with a 5,232-row reduced version. Recovered by copying the canonical 5,917-row master from the OneDrive backup at `/Users/Alex/Library/CloudStorage/OneDrive-Personal/Documents/Claude/Projects/Achievement/`. Then re-ran rebuild.command.
8. ✅ Added an `⚠ AUDIT WARNING` header to `build_master_demographics.py` so it isn't run blindly until the MI Troy input divergence is reconciled.
9. ✅ Created per-prompt capsules for all five in-session prompts (17-00-01, 17-05-00, 17-10-00, 17-15-00, 17-20-00), each with a `Cache and Tools_{timestamp}/` snapshot of the 12-file tooling at this moment.

## What's still open

- `Working Folder/_OLD_JUNK_DELETE_ME/` — leftover empty directory from a copy-glitch in the previous OneDrive session. Sandbox can't unlink the macOS-locked `.DS_Store` files inside; user needs to delete via Finder.
- `Working Folder/Cache and Tools/_PYCACHE_DELETE_ME/` — same situation, `__pycache__` from script execution that I had to rename rather than delete.
- `achievement_project/scripts/` — still contains the original (pre-port) versions of the build scripts, now redundant with the canonical copies in `Cache and Tools/`. Left in place to avoid breaking unknown external references; can be removed once you've confirmed the new layout is good.
- `build_master_demographics.py` MI-Troy input drift — flagged with audit warning, not fixed.
- The git repo has all of this as untracked changes; no commits made (no instruction to commit).

## Cross-references

- Substantive instruction: `prompt_2026-04-28_17-05-00.md`
- Tooling inventory: `Working Folder/Cache and Tools/README.md`
- Bellevue fix audit trail: `Working Folder/CHANGELOG.md`

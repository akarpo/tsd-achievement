# Prompt — 2026-04-28 17:45:00 (synthetic timestamp)

> Now I want you to change the build process so that generated files are placed in the root project folder only, not in Cache and Tools. I also want you to modify index.html so that all Districts are selected for the first graph, and only 'All Students' is selected

## Two independent changes

1. **Build process: root-only output.** Until now the build wrote a canonical xlsx at `Cache and Tools/dashboard/`, then copied it to `Working Folder/` (handoff) and the repo root (deploy). User wants ONE copy — at the repo root only.

2. **Dashboard defaults.** `index.html` had hardcoded JS defaults selecting only 3 districts (Troy, Bellevue, Coppell) and 2 subgroups (All Students, SWD). User wants all 8 districts selected and only "All Students".

## Action

### Build process change

- Edited `_paths.py`: `DASHBOARD_XLSX` and `DASHBOARD_HTML` now resolve to `REPO_ROOT/`. Removed the `DASHBOARD` constant pointing at the (now-gone) `Cache and Tools/dashboard/` folder.
- Moved the canonical files out of `Cache and Tools/dashboard/` to repo root (overwriting the deploy copies). Renamed the now-empty `dashboard/` to `_DASHBOARD_DELETE_ME/` (sandbox couldn't `rmdir` due to a residual `.DS_Store`).
- Removed the handoff copies in `Working Folder/` (renamed to `_index_html_DELETE_ME` and `_xlsx_DELETE_ME` — same lock issue, user deletes via Finder).
- Rewrote `rebuild.command` — two steps now (install openpyxl, run `build_master_xlsx.py`), no copy step. The script writes directly to the repo root because `_paths.DASHBOARD_XLSX` now points there.
- `build_master_xlsx.py` and `patch_xlsx_in_place.py` needed no changes — they import `DASHBOARD_XLSX` from `_paths`, so they automatically retarget.

### index.html defaults

- Edited lines 165-166 in `Cache and Tools/dashboard/index.html` (the canonical, just before move):
  - `selectedDistricts: new Set(['Troy SD', 'Bellevue SD', 'Coppell ISD'])` → all 8 districts
  - `selectedSubgroups: new Set(['All Students', 'SWD'])` → just `['All Students']`
- The 8 districts canonical list (matching the embedded data payload, alphabetical): `['Bellevue SD', 'Coppell ISD', 'Dublin USD', 'Milpitas USD', 'Palo Alto USD', 'Troy SD', 'Walnut Valley USD', 'West Windsor-Plainsboro']`.

## Validation

- Renamed root xlsx to `.before`, ran `rebuild.command`, verified a fresh xlsx was written directly to repo root (new MD5 hash, fresh mtime).
- All Demographics: 5,497 rows. Bellevue 2025 G3 Math All Students = N=1,345, 69.4% (the corrected SBAC value, preserved through the build-process change).
- `find . -name "G3-G7_Achievement_Data.xlsx" -not -path '*_DELETE_ME*'` returns exactly one path: `./G3-G7_Achievement_Data.xlsx`. Same for index.html.

## Final layout

```
Achievement/
├── index.html                        ← only copy (static, with new defaults)
├── G3-G7_Achievement_Data.xlsx       ← only copy (generated by rebuild)
├── LICENSE, README.md
└── Working Folder/
    ├── README.md, CHANGELOG.md
    ├── Cache and Tools/              (no more dashboard/ subfolder)
    │   ├── _paths.py, rebuild.command, README.md
    │   ├── *.py (10 scripts)
    │   ├── extracted_data/
    │   ├── source_data/
    │   ├── spreadsheets/
    │   └── project_docs/
    └── Prompts/
```

## Cleanup left for user (Finder)

- `Working Folder/_index_html_DELETE_ME` (the handoff copy)
- `Working Folder/_xlsx_DELETE_ME` (the handoff copy)
- `Working Folder/Cache and Tools/_DASHBOARD_DELETE_ME/` (the now-empty canonical folder)
- `_BEFORE_REBUILD_xlsx_DELETE_ME` at repo root (the staging file for my build verification)
- Plus the prior session's leftover delete-me folders (`_ACHIEVEMENT_PROJECT_DELETE_ME`, `_OLD_JUNK_DELETE_ME`, `_PYCACHE_NEW_DELETE_ME`)

## Cross-references

- Updated tooling inventory: `Working Folder/Cache and Tools/README.md`
- Audit trail: `Working Folder/CHANGELOG.md`

# Prompt — 2026-04-28 18:00:00 (synthetic timestamp)

> The download source data is in two places - delete mention of it from the top

## Action

Two `Download...G3-G7_Achievement_Data.xlsx` mentions in `index.html`:

- **Line 74 (header)** — small button beneath the title meta line: `⬇ Download source data (G3-G7_Achievement_Data.xlsx, 234 KB)`. **Removed.**
- **Line 144 (body, inside the `<h2>Download the data</h2>` section)** — full-size accent-colored button: `📥 Download G3-G7_Achievement_Data.xlsx (229 KB)`. **Kept.**

The dedicated download section in the body is the right home for this; the header button was redundant.

## Verification

`grep` for `Download.*data|G3-G7_Achievement_Data\.xlsx` in `index.html` now returns exactly the two body lines (the section heading + the button). The header is gone.

## Cross-references

- Audit trail: `Working Folder/CHANGELOG.md`

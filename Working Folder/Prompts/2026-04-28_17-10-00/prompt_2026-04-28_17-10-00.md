# Prompt — 2026-04-28 17:10:00 (synthetic timestamp)

> "One ambiguity I want to flag: you wrote 'Working Files folder' but the existing one is named 'Working Folder'. I'm assuming typo and using `Working Folder`. Tell me if you'd rather rename it to `Working Files`."
>
> Correct.
>
> "One concern to acknowledge: the underlying OneDrive/Bash path issue may not fully go away with restructuring — Bash in this session does have a mounted view (`/sessions/eager-gracious-dirac/mnt/Achievement/`), so I can try the xlsx rebuild via the mount. If the OneDrive sync agent has a lock on the old xlsx, the rebuild will fail and I'll fall back to the surgical `patch_xlsx_in_place.py` approach."
>
> You're not working with the OneDrive folder anymore. You're working in /Users/Alex/Dev/Github/Achievement.
>
> Does this answer all your questions?

## What this resolved

1. **"Working Folder" name confirmed** — typo, no rename needed. All restructuring uses `Working Folder/`.
2. **OneDrive concern obsoleted** — user moved the working copy of the project from OneDrive to a local git-tracked repo at `/Users/Alex/Dev/Github/Achievement`. No more OneDrive sync-agent locks. Bash works cleanly through the new mount.

## Action taken

Requested directory access via `mcp__cowork__request_cowork_directory(path="/Users/Alex/Dev/Github/Achievement")` — granted. Bash mount path: `/sessions/eager-gracious-dirac/mnt/Github--Achievement`.

## Two open clarifications I flagged in response

1. Master `Cache and Tools/` AND per-prompt snapshots — both?
2. The historical `.md` prompt files — retro-fold into `Prompts/{timestamp}/` capsules, or leave loose?

Both got answered in the next capsule (`17-15-00`).

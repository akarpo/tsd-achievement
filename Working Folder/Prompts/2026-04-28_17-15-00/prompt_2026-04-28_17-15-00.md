# Prompt — 2026-04-28 17:15:00 (synthetic timestamp)

> "Master `Cache and Tools/` AND per-prompt snapshots — both? My read was: one master at `Working Folder/Cache and Tools/` (canonical, evolves), plus per-prompt timestamped snapshots inside each prompt capsule (frozen). Confirm or correct."
>
> Yes, that's correct.
>
> "The historical `.md` prompt files at the old project root — do you want them retro-folded into `Working Folder/Prompts/` as proper capsules (one folder per timestamp), or just dropped loose into `Working Folder/`? Capsule treatment is more consistent with the new model; loose is faster."
>
> Yes, one folder per timestamp.
>
> "Bash at the new location. My bash sandbox only has the OneDrive path mounted right now. Once you approve `/Users/Alex/Dev/Github/Achievement`, I'll test whether bash can reach it through the new mount — that determines whether the xlsx rebuild runs natively at the Dev/Github location or whether I have to stage files into the sandbox, build there, and copy back. Either way works; just affects the path I take."
>
> Yes, mount the new directory I shared with you

## What this resolved (the three rules-of-the-system locked in here)

1. **Two-tier Cache and Tools/** — one canonical master at `Working Folder/Cache and Tools/` (which evolves over time), plus a frozen per-prompt snapshot inside every prompt capsule. The master is the live tooling; the snapshots are version-controlled-by-time tracebacks.

2. **Historical .md prompts get retro-folded** — every `prompt_*.md` file at the old project root becomes its own `Working Folder/Prompts/{timestamp}/{filename}.md` capsule, even for prior sessions. This makes the model uniform: every prompt is a folder.

3. **Bash works at the new location** — the new mount path is `/sessions/eager-gracious-dirac/mnt/Github--Achievement`. End of OneDrive blocker.

## Action taken

Confirmed bash access by listing the new location's contents. Created the new `Working Folder/Cache and Tools/` and `Working Folder/Prompts/` skeleton. Then proceeded with the structural moves laid out in `17-05-00`.
